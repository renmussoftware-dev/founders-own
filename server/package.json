{
  "name": "founders-own-advisor-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Serverless backend for the Founders Own weekly AI deep-dive (calls Claude; holds the Anthropic key off-device).",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.113.0"
  },
  "devDependencies": {
    "@vercel/node": "^5.0.0",
    "typescript": "^5.6.0"
  }
}
